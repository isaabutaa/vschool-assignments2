import React from "react"

export default function Footer(props) {
    return (
        <footer className={`footer-container ${props.theme}`}>
            <p>Place feet here please</p>
        </footer>
    )
}